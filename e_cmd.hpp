#ifndef E_CMD_HPP
# define E_CMD_HPP

enum class e_cmd : string {
    START = "start"
    STOP = "stop"
    RESTART = "retart"
    STATUS = "status"
    RELOAD = "update"
    STOP_MAIN_PROGRAM = "quit"
}

# endif